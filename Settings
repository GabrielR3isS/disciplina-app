import React from "react";
import { motion } from "framer-motion";
import { Calendar, Bell, Shield, User, ChevronRight, LogOut } from "lucide-react";
import { Link } from "react-router-dom";
import { createPageUrl } from "@/utils";
import { base44 } from "@/api/base44Client";

const settingsOptions = [
  {
    icon: Calendar,
    label: "Integração de Calendário",
    description: "Google Calendar e Outlook",
    page: "CalendarSettings",
    color: "text-violet-400",
    bgColor: "bg-violet-500/20"
  },
  {
    icon: Bell,
    label: "Notificações",
    description: "Configurar lembretes e alertas",
    page: "Notifications",
    color: "text-blue-400",
    bgColor: "bg-blue-500/20"
  },
  {
    icon: Shield,
    label: "Apps Bloqueados",
    description: "Gerenciar bloqueios",
    page: "BlockedApps",
    color: "text-emerald-400",
    bgColor: "bg-emerald-500/20"
  }
];

export default function Settings() {
  const handleLogout = async () => {
    await base44.auth.logout();
  };

  return (
    <div className="min-h-screen bg-slate-950 pb-24">
      {/* Header */}
      <div className="px-5 pt-12 pb-6">
        <motion.div
          initial={{ opacity: 0, y: -20 }}
          animate={{ opacity: 1, y: 0 }}
        >
          <h1 className="text-2xl font-bold text-white">Configurações</h1>
          <p className="text-slate-400 text-sm mt-1">Personalize seu app</p>
        </motion.div>
      </div>

      <div className="px-5 space-y-4">
        {/* Settings Options */}
        <div className="space-y-2">
          {settingsOptions.map((option, index) => (
            <motion.div
              key={option.page}
              initial={{ opacity: 0, y: 20 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ delay: index * 0.1 }}
            >
              <Link
                to={createPageUrl(option.page)}
                className="flex items-center gap-4 p-4 rounded-2xl bg-slate-900/50 border border-white/5 hover:bg-slate-800/50 transition-all group"
              >
                <div className={`w-12 h-12 rounded-2xl ${option.bgColor} flex items-center justify-center`}>
                  <option.icon className={`w-6 h-6 ${option.color}`} />
                </div>
                <div className="flex-1 min-w-0">
                  <p className="text-white font-medium">{option.label}</p>
                  <p className="text-slate-500 text-sm">{option.description}</p>
                </div>
                <ChevronRight className="w-5 h-5 text-slate-500 group-hover:text-violet-400 transition-colors" />
              </Link>
            </motion.div>
          ))}
        </div>

        {/* Account Section */}
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ delay: 0.4 }}
          className="pt-4"
        >
          <h3 className="text-slate-400 text-xs uppercase tracking-wider font-medium mb-3 px-1">
            Conta
          </h3>
          <button
            onClick={handleLogout}
            className="w-full flex items-center gap-4 p-4 rounded-2xl bg-red-500/10 border border-red-500/20 hover:bg-red-500/20 transition-all group"
          >
            <div className="w-12 h-12 rounded-2xl bg-red-500/20 flex items-center justify-center">
              <LogOut className="w-6 h-6 text-red-400" />
            </div>
            <div className="flex-1 text-left">
              <p className="text-red-400 font-medium">Sair</p>
              <p className="text-red-400/70 text-sm">Desconectar da conta</p>
            </div>
            <ChevronRight className="w-5 h-5 text-red-400 group-hover:translate-x-1 transition-transform" />
          </button>
        </motion.div>

        {/* App Info */}
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ delay: 0.5 }}
          className="p-4 rounded-2xl bg-slate-800/30 border border-slate-700/50 text-center"
        >
          <p className="text-slate-400 text-xs">
            <strong className="text-slate-300">FocusLock</strong> v1.0.0
          </p>
          <p className="text-slate-500 text-xs mt-1">
            Discipline is freedom 🎯
          </p>
        </motion.div>
      </div>
    </div>
  );
}
